% stage 0 pre-screens noisy spans so downstream callers do not chase broadband clutter.
% it runs before feature extraction completes and hands off masks to the detector stack.
% this folder will grow with gating, tonality guards, and any shared helpers for the noise stage.
